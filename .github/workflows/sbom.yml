name: Generate SBOM

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  generate-sbom:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate SBOM for Frontend
        uses: anchore/sbom-action@v0
        with:
          path: ./frontend
          format: spdx-json
          output-file: frontend-sbom.spdx.json

      - name: Generate SBOM for Backend
        uses: anchore/sbom-action@v0
        with:
          path: ./backend
          format: spdx-json
          output-file: backend-sbom.spdx.json

      - name: Generate SBOM for Contracts
        uses: anchore/sbom-action@v0
        with:
          path: ./contracts
          format: spdx-json
          output-file: contracts-sbom.spdx.json

      - name: Upload Frontend SBOM Artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-sbom
          path: frontend-sbom.spdx.json

      - name: Upload Backend SBOM Artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-sbom
          path: backend-sbom.spdx.json

      - name: Upload Contracts SBOM Artifact
        uses: actions/upload-artifact@v4
        with:
          name: contracts-sbom
          path: contracts-sbom.spdx.json

      - name: Attach SBOMs to Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            frontend-sbom.spdx.json
            backend-sbom.spdx.json
            contracts-sbom.spdx.json
